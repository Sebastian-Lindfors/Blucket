@model ArticleIndexVm
@inject Blocket.Services.UserService UserService
@{
    var isAdmin = await UserService.IsCurrentUserAdministratorAsync();
}

<h1>Article Index!</h1>

<a asp-action="Create">Add Article</a> <a asp-action="Delete">Delete Article</a>
<hr>
<table class="table">
    <thead>
        <tr>
            <th>
                Id
            </th>
            <th>
                Name
            </th>
            <th>
                Description
            </th>
            <th>
                Price
            </th>
            <th>
                Published
            </th>
            @if (isAdmin)
            {
                <th></th>
            }
        </tr>
    </thead>
    <tbody>
        @foreach (var article in Model.Articles)
        {
            <tr>
                <td>
                    @article.Id
                </td>
                <td>
                    @article.Name
                </td>
                <td>
                    @article.Description
                </td>
                <td>
                    @article.Price
                </td>
                <td>
                    @article.Published
                </td>

                @if (isAdmin)
                {
                    <td>
                        <a asp-action="Delete" asp-route-id="@article.Id" class="btn btn-danger">Delete</a>
                    </td>
                }
            </tr>
        }
    </tbody>
</table>